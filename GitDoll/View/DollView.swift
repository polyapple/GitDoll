

import SwiftUI

struct DollView: View {
    var body: some View {
        Circle()
            .frame(width: 200)
    }
}

struct DollView_Previews: PreviewProvider {
    static var previews: some View {
        DollView()
    }
}
